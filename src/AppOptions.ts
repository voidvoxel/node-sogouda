export default interface AppOptions {
    debug: boolean | null,
    height: number | null,
    frameless: boolean | null,
    title: string | null,
    url: string | null,
    width: number | null
}
